name: Trigger
on:
  issue_comment:                                     
    types: [created, edited]
jobs:
  job01:
    if: contains(github.event.comment.body, '/pull/')    # check if the comments come from pull request, exclude those from issue.
    runs-on: [ubuntu-latest]
    steps:
      - name: say hello
        if: contains(github.event.comment.body, '/support')   # check the comment if it contains the keywords
        run: |
          echo 'triggered'
          exit 0
